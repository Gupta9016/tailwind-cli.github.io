# Local to remote repo:
 *Method :1
 git -repo create
 git clone <link>
 git add ./ git add filename
 git commit -m "msg"
 git push origin  main

*method :2
git init
<paste link>
git add .
git commit -m "tailwind-cli"
git push origin main
